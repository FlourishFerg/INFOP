# =========================================================
# InfoPouch Backend - Production Environment Variables
# =========================================================
# Copy the values from here into /home/ubuntu/infopouch/app.env
# on your Oracle Cloud VM after running scripts/setup.sh
# =========================================================

SPRING_PROFILES_ACTIVE=prod

# ----------------------------------------------------------
# Database (from Neon.tech dashboard -> Connection Details)
# Format: jdbc:postgresql://<host>/<dbname>?sslmode=require
# ----------------------------------------------------------
DATABASE_URL=jdbc:postgresql://ep-xxx-xxxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_USERNAME=neondb_owner
DATABASE_PASSWORD=your-neon-password

# ----------------------------------------------------------
# JWT — must be at least 32 characters, keep this secret
# Generate one: openssl rand -hex 32
# ----------------------------------------------------------
JWT_SECRET=replace-with-a-long-random-secret-minimum-32-characters

# ----------------------------------------------------------
# App base URL — used in verification/reset email links
# Use your Oracle VM public IP until you have a domain
# ----------------------------------------------------------
APP_BASE_URL=http://your-vm-public-ip:8080
